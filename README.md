# F1 Neural Network

## Installation

1. Clone the repo:
   `git clone https://github.com/your-username/F1_NN.git`

2. Create a virtual environment:
   `python -m venv venv`

3. Activate it:
   `source venv/bin/activate`

4. Install requirements:
   `pip install -r requirements.txt`